"""pmsearch: PubMed batch search and Excel export.

Run from the repo: ``python -m pmsearch …``. After editable install (``pip install -e <repo>``),
the ``pmsearch`` command is on ``PATH`` from any working directory.
"""

__version__ = "0.1.0"
